# 逐字歌词（Karaoke 时间轴）— 实现策划书

> 状态：策划书 · 2026-08-13
> 路线：**JS 分析引擎**（浏览器原生解码 + Web Worker FFT + IndexedDB 缓存）
> 结论先行：**可行**。逐字渲染、分词、缓存、后台分析都能干净落地；但 FFT 起音检测是**启发式**，精度上限 =「视觉流畅、节奏大致对齐」的主流 karaoke 水准，达不到「完全精准」（完全精准需 ASR 强制对齐，见风险节）。

---

## 一、目标

- 歌词按字/词高亮，随播放推进逐字点亮（Apple Music 式）。
- 首次播放立即有逐字效果（粗排），后台分析完成后无缝升级为精排并缓存。
- 第二次播放 / 重听时，从第 0 秒即精排（缓存命中）。
- 严格不破坏现有歌词行滚动布局（参考实现动效是硬约束）。

## 二、总体架构（JS 路线）

```
点击播放
 ├─ Intl.Segmenter 分词 → 比例粗排 → 立即逐字渲染 (<1ms，零等待)
 ├─ 查 IndexedDB
 │   ├─ 命中 → 用缓存精确时间轴替换渲染
 │   └─ 未命中 → 后台触发音频分析（async，不阻塞 UI）
 │          └─ 完成 → 写缓存 → 刷新当前行
 └─ 第二次播放 → 缓存命中 → 直接精确
```

- **解码在主线程**（`OfflineAudioContext.decodeAudioData`，浏览器媒体线程异步，不卡 UI），**FFT 分析在 Web Worker**（PCM 用 transferable 转过去）。
- 在线歌曲与本地歌曲同路径：本地读 asset 字节，在线 `fetch(歌曲url)` → `decodeAudioData`。分析的就是 `<audio>` 正在播的同一 URL。

## 三、前端：分词与逐字渲染

### 分词（`Intl.Segmenter`，WebView2 支持）
- `granularity: "grapheme"` 拆开 → CJK 每字一个单元；连续 `[A-Za-z0-9'’-]` 合并为词；空格丢弃。
- 无 `Intl.Segmenter` 时降级 `Array.from(text)`。
- 中英混排天然支持（"We 我们" → `["We","我","们"]`）。

### 数据模型（向后兼容，`units` 可选）
```ts
interface WordUnit { text: string; start: number; end: number }
interface LyricLine {
  time: number;
  text: string;
  translation?: string;
  units?: WordUnit[];   // 新增，可选
}
```

### 渲染（`LyricsView.vue`）
- **只改当前行文本**为逐字 `<span>`，行滚动/级联/模糊逻辑一律不动。
- 用 `sungWordIndex` computed 计算「已唱到第几个字」：
  - 依赖 `currentTime + activeLine + lyrics`，**值不变时不触发重渲染**（Vue computed 缓存），只在跨字边界时刷新 → 高效。
- CSS：当前行未唱的字半透明、已唱的字全亮，`transition: color` 平滑。翻译行不逐字。
- 后续可升级「当前字内填充进度」（Apple Music 式进度条）。

## 四、时间轴引擎（粗排 → 精排）

- **粗排（instant）**：行 `[start, end]`，`end = 下一行 start`（末行按字数估算）。将 N 个 token 均分到前 85% 时长，末尾 15% 留作尾音停顿。零分析、零延迟。
- **精排（cached）**：来自分析结果，按字替换。
- 引擎统一输出 `units`，渲染层无感 → 分析完成时**无缝替换**。
- Phase 2 增加 `applyPreciseTimeline(lines, cache)` 用缓存覆盖粗排。

## 五、Phase 2：分析引擎（JS）

### 数据流
1. 读整曲 PCM：`fetch/read → decodeAudioData`（单声道下采样，长曲只取前 5 分钟兜底）。
2. 转给 Web Worker：PCM Float32Array（transferable，零拷贝）。
3. Worker 内 FFT 谱通量起音检测（`rustfft` 的 JS 等价物：手写 radix-2 约 40 行，或引 `fft.js`）。

### 算法（每行已知 `[t0,t1]`、N 个字 → 求 N-1 个内部边界）
1. ~20ms 帧算**谱通量**（相邻帧频谱正差和）或 RMS 能量。
2. 只保留 **300Hz–3kHz 人声频带**（削弱鼓/贝斯干扰）。
3. 平滑 + 自适应阈值峰值拾取（带最小间隔）→ 候选起音。
4. 候选 ≥ N-1 取 N-1 个；不足则缺口按比例补齐（单调递增约束）。

> 关键：**已知边界数 + 已知窗口**让「行内切分」比「盲切整曲」可解得多，这是方案可行的核心。

## 六、缓存层（IndexedDB）

- 原生 `indexedDB` 包一层小工具（`src/utils/wordCache.ts`），无第三方依赖。
- store `words`：key = 歌曲标识（本地 = 文件 id/路径 hash；在线 = `server:id`），value = `{ v: 1, lines: [{ idx, times: number[] }] }`。
- 读写在 Web Worker 或主线程均可（Worker 支持 indexedDB）。

## 七、触发与降级

- 首次播放：粗排 + 后台分析（**本地文件先做分析；在线歌曲可先用粗排**，缩小首版范围）。
- 完成：写缓存 + 刷新当前行。
- 再次播放：缓存命中直接精排。
- 分析失败 / 格式不支持 / 超长曲目：静默降级粗排，不打扰播放。

## 八、分阶段实施

- **Phase 1（本阶段，纯前端 MVP）**：分词 + 粗排时间轴 + 逐字高亮。零分析、全曲立即可用、本地可验证。
- **Phase 2**：JS 分析引擎 + Web Worker + IndexedDB 缓存 + 渐进增强。
- **Phase 3（可选/未来）**：真·精准走 ASR 词级时间戳（Whisper 等）。

## 九、涉及文件

- `shared/types.ts`（`LyricLine` 加 `units`，新增 `WordUnit`）
- `src/utils/lyricTimeline.ts`（新：分词 / 粗排 / 时间轴引擎）
- `src/utils/wordCache.ts`（新，Phase 2：IndexedDB）
- `src/workers/wordAnalysis.worker.ts`（新，Phase 2）
- `src/stores/player.ts`（parseLrc 附加 units；触发分析）
- `src/components/LyricsView.vue`（逐字渲染，行布局不动）

## 十、风险与降级预期

1. **精度上限（最大）**：FFT 起音是启发式，伴奏/弱声母/长音会误切或漏切，达不到「完全精准」，须接受为主流 karaoke 水准。
2. **LRC 行时间本身有误差**——字建立在行上，行不准字也不准。
3. **内存**：`decodeAudioData` 整曲 PCM 载入（3.5 分钟约 20–40MB），分析完释放；超长曲加「只分析前 5 分钟」兜底。
4. **在线歌曲 = 二次下载**：分析需再 fetch 一次完整音频（1–3s，网络决定），写缓存后不重复。
5. **LyricsView 动效约束**：逐字只动当前行文本，勿碰行滚动/级联/模糊逻辑。

## 附：JS vs Rust 分析引擎对比（决策依据）

| 维度 | JS（decodeAudioData + Worker） | Rust（symphonia + rustfft） |
|---|---|---|
| 解码 | 浏览器原生，与播放同一解码器，格式全覆盖 | symphonia 格式略少、mp3 慢 |
| 在线歌曲 | `fetch → decodeAudioData` 一行 | URL 需下载落盘再解 |
| 后台线程 | 主线程解码 → Worker FFT | Tauri async command（要 spawn_blocking） |
| 编译风险 | 零新增 Rust 依赖，本地可验证 | 新增依赖，本机无 MSVC 只能靠 CI |
| 时序一致性 | 与播放同一解码器，天然同源 | 独立时钟，需校正（实际偏差可忽略） |

**结论：JS**——在线歌曲是刚需、零编译风险、算法才是精度瓶颈与语言无关。Rust 仅在「批量分析整个曲库 / 非浏览器环境」时值得。
